Prerequisites: This project is built in React, and needs nodeJS installed to run properly

Run 'npm install' to install all needed dependencies
Run 'npm start' to access project on 'http://localhost:3000'